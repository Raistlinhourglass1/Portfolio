# Dependencies
node_modules/

# Environment variables (secrets, DB paths, etc.)
.env
.env.local

# Database files (don't commit actual data, just schema/migrations)
*.sqlite
*.sqlite3
*.db
*.sqlite-shm
*.sqlite-wal

# Auto-generated secret used to salt visitor-IP hashes (server/db/analytics.js).
# Lives with the analytics DB on its volume in prod; must never be committed.
analytics-salt

# Logs
logs/
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp

# Build output
dist/
build/

# Large media (host separately on DAS, reference via path/URL instead)
server/public/images/originals/
server/public/videos/
